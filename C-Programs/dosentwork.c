#include<stdio.h>
#include<conio.h>
void main(){
    int dist[20],k,i,mo[20],n;
    char a[20];
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%s",a[i]);
        printf("%s",a[i]);
        scanf("%d%d",&dist[i],&mo[i]);
    }

}
